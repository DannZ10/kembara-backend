; Populate $_ENV from the real process environment. Laravel's env() reads
; $_ENV/$_SERVER (not getenv), so without this, PaaS-injected variables
; (Render, Fly, Cloud Run) are invisible and the bundled .env defaults win.
variables_order = "EGPCS"

memory_limit = 256M
